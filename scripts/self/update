#!/usr/bin/env bash

set -euo pipefail

source "$DOTFILES_PATH/scripts/core/_main.sh"

##? Update DOTFILES to the latest stable release
##?
##? Usage:
##?    update
docs::parse "$@"

cd "$DOTFILES_PATH"
git restore .
git checkout main
git pull
git submodule update --init --recursive
chmod -R 755 $DOTFILES_PATH

output::answer '✅ DOTFILES updated to the latest version'