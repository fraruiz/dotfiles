#!/usr/bin/env bash

set -euo pipefail

source "$DOTFILES_PATH/scripts/core/_main.sh"

##? Apply all symlinks
##?
##? Usage:
##?    apply
##?
docs::parse "$@"

source "$DOTFILES_PATH/symlinks/links.sh"

if platform::is_macos; then
  if platform::is_macos_arm; then
    source "$DOTFILES_PATH/symlinks/links_macos.sh"
  else
    source "$DOTFILES_PATH/symlinks/links_macos_intel.sh"
  fi
else
  source "$DOTFILES_PATH/symlinks/links_linux.sh"
fi

log::success "Done!"
